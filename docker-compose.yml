version: '3'
services:

  redis-primary:
    image: redis:7.2.2
    command: redis-server --appendonly yes --maxmemory 8mb --maxmemory-policy allkeys-lru
    volumes:
      - ~/data/redis:/data
    deploy:
      restart_policy:
        condition: none
      placement:
        constraints: [node.role == manager]

  redis-replica:
    image: redis:7.2.2
    command: redis-server --replicaof redis-primary 6379
    volumes:
      - ~/data/redis:/data
    deploy:
      restart_policy:
        condition: none
      placement:
        constraints: [node.role == worker]
    depends_on:
      - redis-primary

  url-handler:
    image: sjwsteven/url-handler:latest
    ports:
      - "8080:5000"
    deploy:
      restart_policy:
        condition: none
    depends_on:
      - redis-primary
      - redis-replica
    healthcheck:
      test: curl --fail http://localhost:5000/sid
      interval: 5s
      retries: 2
      start_period: 20s
      timeout: 5s